#include <stdio.h>

/**
 *main - Entry Point
 *
 * Return: Always 0 success
 */

int main(void)
{
	char c = 'a';

	while (c <= 'a')
	{
	if (c != 'q' || c != 'e')
	{
	putchar(c);
	}
	}

	return (0);
}
