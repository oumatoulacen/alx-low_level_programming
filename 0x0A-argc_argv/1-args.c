#include <stdlib.h>
#include "main.h"

/**
 * main - check the code
 * @argc: number of args
 * @argv: array of size argc
 * Return: always 0
 */

void main(argc, argv)
{
	int num = 0;
	while (argv[i])
		num++;
	_putchar(num + 48);
	_putchar('\0');
	return (0);
}
